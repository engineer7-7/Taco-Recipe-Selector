<!DOCTYPE html>
<html lang="">

<head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
<h1>Taco Town 🌮</h1>

<form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
</form>
<div id="recipeContainer" class="hidden">

    <% if (hasRecipe) { %>
        <h2><%= recipe[id]["name"] %></h2>

        <h3>Ingredients</h3>
        <ul id="ingredientsList">
            <li><%= recipe[id]["ingredients"]["protein"]["name"] %>
                ,<%= recipe[id]["ingredients"]["protein"]["preparation"] %></li>
            <li>
                <%= recipe[id]["ingredients"]["salsa"]["name"] %>
            </li>
            <li>
                <%= recipe[id]["ingredients"]["toppings"][0]["quantity"] %>
                of <%= recipe[id]["ingredients"]["toppings"][0]["name"]
                %>
            </li>
            <li>
                <%= recipe[id]["ingredients"]["toppings"][1]["quantity"] %>
                of <%= recipe[id]["ingredients"]["toppings"][1]["name"]
                %>
            </li>
            <li>
                <%= recipe[id]["ingredients"]["toppings"][2]["quantity"] %>
                of <%= recipe[id]["ingredients"]["toppings"][2]["name"]
                %>
            </li>

        </ul>

    <% } else { %>
        <!-- If no recipe yet, show: -->
        <h2>Pick your favourite taco ingredient. 👆</h2>
        <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->

        <h3>Ingredients:</h3>
        <ul id="ingredientsList">
            <li>Protein name, protein preparation</li>
            <li>Salsa name</li>
            <li>Every other topping</li>
        </ul>

    <% } %>

</div>
</body>

</html>